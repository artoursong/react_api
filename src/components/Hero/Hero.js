import * as React from 'react';

const Hero = (props) => {
    return (
        <section>
            <img class="w-full h-[800px]" src="/hero-img.jpg" alt="hero-img"/>
        </section>
    );
};

export default Hero;